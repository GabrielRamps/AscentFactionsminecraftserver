# Project coordinates
group=gg.ascent
version=0.1.0-SNAPSHOT

# Paper pin (PRD 6.2: pin the exact build). Update with server/update-paper.sh.
paperVersion=26.2
paperBuild=121
paperApiVersion=26.2.build.121-stable
# Value written into plugin.yml api-version.
paperApiLevel=26.2

# Toolchain
javaVersion=25

# Where copyToServer puts the jar. Override with -PserverDir=... or ASCENT_SERVER_DIR.
serverDir=

# Gradle
org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
