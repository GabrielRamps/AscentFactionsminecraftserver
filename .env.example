# Copy to .env and fill in. .env is git-ignored. Generate passwords with: openssl rand -base64 24

# MariaDB
DB_ROOT_PASSWORD=change-me-root
DB_NAME=ascent
DB_USER=ascent
DB_PASSWORD=change-me
DB_PORT=3306

# Redis
REDIS_PASSWORD=change-me-redis
REDIS_PORT=6379
