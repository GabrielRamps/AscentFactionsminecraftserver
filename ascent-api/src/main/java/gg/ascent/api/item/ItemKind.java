package gg.ascent.api.item;

/** What a tagged item is. Mirrors the {@code items.kind} enum in PRD §6.3. */
public enum ItemKind {
  BOOK_UNOPENED,
  BOOK_OPENED,
  WHITE_SCROLL,
  MAGIC_DUST,
  SPAWNER,
  GEAR,
  KIT_ITEM,
  XP_BOTTLE,
  LOOTBAG
}
